(*******************************************************************************
    Copyright 2008 Sun Microsystems, Inc.,
    4150 Network Circle, Santa Clara, California 95054, U.S.A.
    All rights reserved.

    U.S. Government Rights - Commercial software.
    Government users are subject to the Sun Microsystems, Inc. standard
    license agreement and applicable provisions of the FAR and its supplements.

    Use is subject to license terms.

    This distribution may include materials developed by third parties.

    Sun, Sun Microsystems, the Sun logo and Java are trademarks or registered
    trademarks of Sun Microsystems, Inc. in the U.S. and other countries.
 ******************************************************************************)

component Xml2

  import FortressAst.{...}
  import List.{...}

  export Xml2

  trait Content
    toString():String
  end

  object Element(info:Header, content:List[\Content\], endTag:String) extends Content 
    toString():String = 
      do
        if startTag.getTag() =/= endTag then
          println "Tags dont match start '" startTag.getTag() "' end '" endTag "'"
        end
        var s:String = "<" info.getTag() ">"
        for x <- seq(content) do 
          s := s x.toString() 
        end
        s := s "</" endTag ">"
        s
      end
  end
  Element(startTag:String) = Element(startTag, emptyList[\Content\](), startTag)
  Element(startTag:String, endTag:String) = Element(startTag, emptyList[\Content\](), endTag)

  object Header(startTag:String, attributes:List[\Attribute\])
    getter getTag():String = startTag
    getter attributes():String = attributes
  end

  object Attribute(key:String, val:String)
    getter getKey():String = key
    getter getValue():String = val
  end

  object CData(v:String) extends Content
    toString():String = v
   end

end
