* Install
  Copy the sub-directories you find here to your ~/.vim directory. Example
  $ cp -a fortress/Vim/syntax ~/.vim/.
  ...

* Use
Add the following line to your ~/.vimrc

  au BufNewFile,BufRead *.fsi,*.fss set ft=fortress

Then start vim and syntax highlighting should work. If it does not try executing

  :syntax enable
